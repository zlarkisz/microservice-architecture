# Домашнє завдання #1: Monolith vs. Microservices

**Курс:** Microservice Architecture
**Завдання:** Оцінити, за якою архітектурою побудовано продукт, над яким ви працюєте
**Проект:** Real-time Multiplayer Chess Application

---

## 1. Тип Архітектури

**Модульний Моноліт (Modular Monolith)** з елементами Full-Stack розділення (Frontend/Backend separation).

> ⚠️ Це **НЕ** мікросервісна архітектура, а класичний моноліт з модульною структурою.

---

## 2. Ознаки, що вказують на цей тип

### 2.1 Структура папок та модулів

```
chess-app/
├── backend/           # Єдиний backend-сервіс
│   └── src/
│       ├── auth/      # Модуль автентифікації
│       ├── game/      # Модуль ігрової логіки
│       ├── user/      # Модуль користувачів
│       ├── prisma/    # Модуль бази даних
│       └── common/    # Спільні типи
│
├── frontend/          # Окремий frontend (Nuxt)
└── docker-compose.yml # Єдина конфігурація розгортання
```

**Висновок:** Модулі (auth, game, user) знаходяться в одному процесі та одній кодовій базі (backend).

### 2.2 Організація залежностей

```typescript
// backend/src/app.module.ts
@Module({
  imports: [
    PrismaModule, // Глобальний модуль БД
    UserModule, // Імпортується напряму
    AuthModule, // Імпортується напряму
    GameModule, // Імпортується напряму
  ],
})
export class AppModule {}
```

**Ознаки моноліту:**

- Модулі зв'язані через прямий імпорт (не через API/HTTP)
- Спільний `PrismaService` для всіх модулів
- Dependency Injection в межах одного процесу

### 2.3 Точки входу (Entry Points)

| Компонент        | Entry Point     | Порт |
| ---------------- | --------------- | ---- |
| Backend (NestJS) | main.ts         | 3001 |
| Frontend (Nuxt)  | Nuxt dev server | 3000 |
| PostgreSQL       | pg_isready      | 5432 |

**Висновок:** Лише один backend-сервіс обробляє всі запити (HTTP + WebSocket).

### 2.4 Комунікація між компонентами

```
Frontend ←→ Backend: HTTP REST + WebSocket (Socket.IO)
Backend modules: Direct method calls (no network)
```

```
┌─────────────────────────────────────┐
│ Backend Process                     │
│ ┌──────────┐  ┌──────────┐          │
│ │AuthModule│→ │GameModule│  (DI)    │
│ └──────────┘  └──────────┘          │
│       ↓            ↓                │
│ ┌─────────────────────────┐         │
│ │ PrismaModule (shared)   │         │
│ └─────────────────────────┘         │
└─────────────────────────────────────┘
```

**Ознаки моноліту:**

- Немає HTTP-комунікації між модулями
- Немає Message Queue (RabbitMQ, Kafka)
- Прямі виклики методів через Dependency Injection

### 2.5 База даних

Єдина спільна база PostgreSQL для всіх модулів:

```prisma
// backend/prisma/schema.prisma
model User { ... }
model Account { ... }
model Session { ... }
model Game { ... }
model Move { ... }
```

**Ознаки моноліту:**

- Одна база даних
- Одна Prisma-схема
- Спільні моделі для всіх модулів
- Немає Database-per-Service паттерну

### 2.6 Docker та Infrastructure конфіги

```yaml
# docker-compose.yml
services:
  backend: # Один backend-сервіс
    build: ./backend
    ports: ["3001:3001"]
    depends_on: postgres

  postgres: # Одна база даних
    image: postgres:16-alpine
```

**Відсутні:**

- ❌ Kubernetes конфіги (k8s manifests)
- ❌ API Gateway (Kong, Nginx as gateway)
- ❌ Service Mesh (Istio, Linkerd)
- ❌ Service Discovery (Consul, Eureka)

### 2.7 Message Brokers та API Gateway

| Компонент        | Наявність    |
| ---------------- | ------------ |
| API Gateway      | ❌ Відсутній |
| Message Broker   | ❌ Відсутній |
| Event Bus        | ❌ Відсутній |
| Service Registry | ❌ Відсутній |

Комунікація відбувається через прямі WebSocket з'єднання (Socket.IO).

---

## 3. Переваги та Недоліки

### ✅ Переваги поточної архітектури

| Перевага                | Опис                                                 |
| ----------------------- | ---------------------------------------------------- |
| Простота розробки       | Один репозиторій, один deployment, простий debugging |
| Низька latency          | Прямі виклики між модулями без network overhead      |
| Транзакційна цілісність | Спільна БД = ACID транзакції між модулями            |
| Простий deployment      | `docker compose up` запускає все                     |
| Швидкий старт проекту   | Не потрібна складна інфраструктура                   |
| Легке тестування        | Інтеграційні тести в одному процесі                  |
| Низька вартість         | Один сервер, менше DevOps overhead                   |

### ❌ Недоліки поточної архітектури

| Недолік                   | Опис                                                    |
| ------------------------- | ------------------------------------------------------- |
| Обмежене масштабування    | Не можна масштабувати окремо GameModule                 |
| Single Point of Failure   | Падіння backend = падіння всього                        |
| Coupling                  | Зміни в одному модулі можуть зламати інші               |
| Deployment ризики         | Деплой всього при зміні одного модуля                   |
| Технологічний lock-in     | Всі модулі на NestJS/TypeScript                         |
| WebSocket state           | In-memory GameRepository не масштабується горизонтально |
| Обмежений fault isolation | Помилка в auth може вбити game processing               |

### 🎯 Для цього проекту (Real-time Chess)

**Архітектура ПІДХОДИТЬ тому що:**

1. Низька latency критична для real-time chess — моноліт швидший
2. Невелика команда — проста архітектура = менше overhead
3. Обмежений scope — один продукт, не платформа
4. WebSocket state — ігрова сесія живе в пам'яті одного процесу

**Проблеми виникнуть при:**

1. 1000+ одночасних ігор — потрібен Redis для shared state
2. Горизонтальне масштабування — потрібен sticky sessions або Redis pub/sub
3. Окреме масштабування AI/Analysis — потрібен окремий сервіс

---

## 4. Порівняльна таблиця

| Критерій            | Значення             | Тип архітектури |
| ------------------- | -------------------- | --------------- |
| Кількість сервісів  | 1 backend            | Моноліт         |
| База даних          | 1 спільна PostgreSQL | Моноліт         |
| Комунікація модулів | Прямі виклики (DI)   | Моноліт         |
| API Gateway         | Відсутній            | Моноліт         |
| Message Broker      | Відсутній            | Моноліт         |
| Deployment          | Один docker-compose  | Моноліт         |
| Модульність коду    | NestJS модулі        | Модульний       |

---

## 5. Чому це НЕ мікросервіси

1. **Один процес** — всі модулі (auth, game, user) запускаються разом
2. **Спільна база даних** — немає Database-per-Service
3. **Синхронна комунікація** — прямі виклики методів замість API/Events
4. **Відсутність інфраструктури** — немає Service Discovery, API Gateway, Message Broker

---

## 6. Рекомендації для еволюції до мікросервісів

Якщо проект виросте, можна виділити:

1. **Game Service** — ігрова логіка + WebSocket
2. **Auth Service** — автентифікація OAuth/JWT
3. **Rating Service** — Glicko-2 розрахунки
4. **Analysis Service** — аналіз партій (CPU-intensive)

При цьому знадобиться:

- Redis для shared WebSocket state
- RabbitMQ/Kafka для event-driven комунікації
- API Gateway (Kong/Nginx)

---

## Висновок

Поточна архітектура **Модульний Моноліт** відповідає потребам проекту та є правильним вибором для MVP.

Мікросервісна архітектура буде **over-engineering** на даному етапі розвитку проекту.

```
Спектр архітектур:

  Monolith ◄────────────────────────► Microservices
     │                                      │
     │    [chess-app]                       │
     │         ↓                            │
     │    Modular          SOA              │
     │    Monolith                          │
     └──────────────────────────────────────┘
```
